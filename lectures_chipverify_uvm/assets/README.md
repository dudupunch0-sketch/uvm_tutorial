# lectures_chipverify_uvm assets

This folder is reserved for local-only assets for the ChipVerify UVM Korean workbook.
The generated HTML pages are self-contained and currently do not require external CSS/JS assets.
